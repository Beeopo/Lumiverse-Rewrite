{
  "name": "rewrite",
  "version": "1.0.3",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun build src/backend.ts --outfile dist/backend.js --target bun --minify && bun build src/frontend.ts --outfile dist/frontend.js --target browser --minify",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
